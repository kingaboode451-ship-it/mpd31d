# منصة إدخال وصل جباية الماء / Water Receipt Entry Portal

واجهة ويب عربية (RTL) متعددة المراحل (Wizard) لإدخال بيانات وصل جباية الماء بشكل احترافي وسلس، مع:
- تقسيم منطقي للبيانات (مشترك، عنوان، قراءات، أقساط، مبالغ، مراجعة)
- احتساب تلقائي للمبلغ الكلي من الحقول الجزئية
- حفظ تلقائي محلي LocalStorage واسترجاع بعد إعادة فتح المتصفح
- معاينة احترافية للوصل جاهزة للطباعة بمقاس تذكرة (حرص جزئي)
- وضع ليلي/نهاري (Dark / Light) بنقرة واحدة
- تحقق أساسي من القيم وإظهار حالات الخطأ/النجاح

## الاستعمال السريع (Quick Start)
1. افتح الملف `index.html` في المتصفح.
2. املأ الحقول في كل خطوة واضغط "التالي".
3. في خطوة "المراجعة" تأكد من صحة البيانات ثم اضغط "إنهاء".
4. ستظهر صفحة الوصل، يمكنك الطباعة عبر زر "طباعة".
5. للتعديل اضغط "تعديل" للعودة إلى النموذج.

## المبلغ الكلي
يُحسب تلقائياً من الحقول:
`waterFee`, `sewerFee`, `garbageFee`, `rawWaterFee`, `meterInstallFee`, `commission`
ويُعرض في الحقل `totalAmount` (للقراءة فقط).

## البنية (Structure)
```
index.html
assets/
  style.css        // التنسيق والسمات، الوضع الليلي، الطباعة، الاستجابة
  app.js           // منطق الخطوات، التخزين، الحساب، الطباعة
logo.png
qr.png
```

## تخصيص (Customization)
- أضف حقولاً جديدة داخل قسم مناسب مع نفس النمط.
- لتضمين حسابات جديدة في المجموع الكلي أضف اسم الحقل إلى المصفوفة `AUTO_TOTAL_FIELDS` في `app.js`.
- لتغيير الألوان عدل متغيرات CSS في :root أو في الوضع الليلي.

## أفكار تطوير مستقبلية (Future Enhancements)
1. ربط بقاعدة بيانات أو API خلفي (Node / Python / .NET) لحفظ السجلات.
2. تصدير PDF مباشرة بدون الاعتماد على طباعة المتصفح (مكتبة مثل jsPDF).
3. إضافة دعم أرقام عربية هندية وتحويل تلقائي.
4. تدقيق إمكانية الوصول (Accessibility) وتحسين تباين الألوان وعلامات ARIA.
5. إنشاء Progressive Web App (PWA) للعمل دون اتصال وتخزين السجلات محلياً.
6. دعم أدوار مستخدمين وصلاحيات.
7. تسجيل عمليات الدخول (Audit Log) لكل تعديل.

## ترخيص
الاستخدام مفتوح داخل نطاق مشروعك؛ لا توجد حقوق إضافية مضافة.

بالتوفيق 🌟
